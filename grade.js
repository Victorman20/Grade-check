var maxscore = 100
function checkscore(){
    var myscore = inputbox.value
    if (myscore >= 45 && myscore <=49 ){
        alert ("Omo mi");
        doc.innerHTML = "<h1>PASS</h1>"
    };
    
    var myscore = inputbox.value
    if (myscore >= 50 && myscore <=59 ){
        alert ("Omo mi");
        doc.innerHTML = "<h1>CREDIT</h1>";
    }
}